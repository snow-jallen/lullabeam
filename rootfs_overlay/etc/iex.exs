# Pull in Nerves-specific helpers to the IEx session
# use Nerves.Runtime.Helpers

# if RingLogger in Application.get_env(:logger, :backends, []) do
#   RingLogger.attach
#   IO.puts """
#   RingLogger is collecting log messages from Elixir and Linux.
#   To decide whether you see the messages, call RingLogger.attach or RingLogger.detach (attached by default).

#   While detached, you can also call:

#     RingLogger.tail
#   """
# end

# Be careful when adding to this file. Nearly any error can crash the VM and
# cause a reboot.
